<?xml version="1.0" encoding="UTF-8"?>
<project name="sweikenb/clutter-datastore" default="list">

    <!-- ============================================  -->
    <!-- Targets: list all targets                     -->
    <!-- ============================================  -->
    <target name="list" description="Displays all available build targets.">
        <exec command="./bin/phing -list" passthru="true"/>
    </target>

</project>
